{
  "name": "glance-proxy",
  "version": "1.0.0",
  "description": "Glance — Claude API proxy server",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "dev": "node --watch proxy.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "marked": "^18.0.4",
    "puppeteer": "^25.0.4"
  }
}
